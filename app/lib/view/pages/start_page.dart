import 'package:flutter/cupertino.dart';

class StartPage extends StatelessWidget {
  const StartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Página em desenvolvimento. Por favor use o menu.',
      ),
    );
  }
}
